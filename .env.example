VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# Optional — live address autocomplete (falls back to curated places when unset)
VITE_MAPBOX_TOKEN=
# Optional — payment goes live only when these are set
VITE_STRIPE_PUBLISHABLE_KEY=
STRIPE_SECRET_KEY=
